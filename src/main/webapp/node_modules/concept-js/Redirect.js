function Redirect() {
}

Redirect.prototype.redirect = function(){
	
	if ($("#redirectForm").length > 0){
		nav.submit($("#redirectForm #nav").val(), $("#redirectForm"), {}, function(resp){
			$("#stage").html(resp);
		});
	}
}